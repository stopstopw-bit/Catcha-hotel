# TASKS.md — แผนงาน Catcha Hotel

> เรียงตามลำดับทำก่อน-หลัง แต่ละข้อจบได้ใน 30–60 นาที
> ช่างสร้าง (builder) ทำ **ทีละข้อ** แล้วเปลี่ยน `- [ ]` เป็น `- [x]` พร้อมจดว่าทำอะไร

## เฟสแรก — MVP ใช้จริงได้

- [ ] ตั้งโครงโปรเจกต์ + หน้าเว็บเปล่า (index) — เสร็จเมื่อ: เปิดหน้าเว็บขึ้นมาเห็นหัวข้อ "Catcha Hotel" ได้
- [ ] ทำโครงเก็บข้อมูลห้อง (เลขห้อง/ประเภท/ราคา/สถานะ) + ใส่ห้องตัวอย่าง 5 ห้อง — เสร็จเมื่อ: เปิดดูรายการห้องทั้ง 5 ได้ ข้อมูลไม่หายเมื่อรีเฟรช
- [ ] ทำหน้ากรอกการจอง (ชื่อ/ติดต่อ/ห้อง/เช็คอิน/เช็คเอาต์) — เสร็จเมื่อ: กรอกแล้วกดบันทึก การจองถูกเก็บและแสดงในรายการ
- [ ] คำนวณจำนวนคืน × ราคา = ยอดรวมอัตโนมัติ — เสร็จเมื่อ: เลือกวัน+ห้องแล้วยอดรวมขึ้นถูกต้องโดยไม่ต้องพิมพ์เอง
- [ ] กันจองซ้อน (ห้องเดียว วันซ้อนกัน จองไม่ได้) — เสร็จเมื่อ: ลองจองห้องเดิมวันทับกัน ระบบเตือนและไม่บันทึก
- [ ] ตรวจข้อมูลก่อนบันทึก (กรอกครบ / วันออกหลังวันเข้า / ยอดไม่ติดลบ) — เสร็จเมื่อ: กรอกผิดกติกาแล้วมีข้อความเตือน ไม่บันทึกมั่ว
- [ ] แดชบอร์ดสรุป: ห้องว่างวันนี้ + เช็คอินวันนี้ + เช็คเอาต์วันนี้ — เสร็จเมื่อ: เปิดหน้าเดียวเห็น 3 อย่างนี้ตรงกับข้อมูลจริง
- [ ] อัปเดตสถานะห้องตามการจอง (จองแล้ว → ไม่ว่าง) — เสร็จเมื่อ: จองห้องแล้วสถานะห้องเปลี่ยนเป็นไม่ว่างในหน้าจัดการห้อง

## เฟสถัดไป — หลัง MVP ผ่านการตรวจ

- [ ] แจ้งเตือนอีเมลไป stopstop.w@ / chatchanok.than@ เมื่อมีจองใหม่ — เสร็จเมื่อ: จองใหม่แล้วทั้ง 2 อีเมลได้รับเมลสรุป
- [ ] หน้าเช็คอิน/เช็คเอาต์ (เปลี่ยนสถานะการจอง) — เสร็จเมื่อ: กดเช็คอิน/เช็คเอาต์แล้วสถานะเปลี่ยนถูกต้อง
- [ ] รายงานสรุปรายเดือน (จำนวนจอง/รายได้) — เสร็จเมื่อ: เลือกเดือนแล้วเห็นยอดรวมถูกต้อง

---

## ❓ คำถามที่ต้องถามเจ้าของธุรกิจ (ตอบก่อนล็อกดีไซน์)
1. ลูกค้าจองเองผ่านเว็บได้ไหม หรือใช้เฉพาะทีมงาน 2 คน?
2. ตอนนี้จดการจองที่ไหน (สมุด/Excel/LINE)? อยากย้ายของเดิมเข้ามาด้วยไหม?
3. โรงแรมมีกี่ห้อง? ประเภทห้องอะไรบ้าง ราคาเท่าไร?
4. ต้องการแจ้งเตือนแบบตั้งเวลา (เช่น สรุปเช็คอินทุกเช้า) ไหม?
5. อยากได้เป็นเว็บแอป หรือทำบน Google Sheet? (ทีมใช้ Gmail อยู่แล้ว)
